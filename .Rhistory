quarto::quarto_inspect()
quarto::quarto_preview_stop()
quarto::quarto_preview()
quarto::quarto_preview_stop()
quarto::quarto_preview()
quarto::quarto_preview_stop()
quarto::quarto_preview()
quarto::quarto_render()
quarto::quarto_preview()
quarto::quarto_preview_stop()
quarto::quarto_render()
quarto::quarto_preview_stop()
quarto::quarto_render()
quarto::quarto_preview()
quarto::quarto_preview_stop()
files <- dir()
iles
files <- dir()
files
files <- dir(pattern='\\.pdf$')
paste0(
'<a href="',
files,
'>',
files,
'</a>',
collapse = '\n'
)
files <- sort(dir(pattern='\\.pdf$'))
paste0(
'<a href="',
files,
'>',
files,
'</a>',
collapse = '\n'
)
quarto::quarto_preview()
files <- sort(dir(pattern='\\.pdf$'))
cat(
paste0(
'<a href="',
files,
'>',
files,
'</a>',
collapse = '\n'
)
files <- sort(dir(pattern='\\.pdf$'))
paste0(
'<a href="',
files,
'>',
files,
'</a>'
)
files <- sort(dir(pattern='\\.pdf$'))
cat(
paste0(
'<a href="',
files,
'>',
files,
'</a>'
)
files <- sort(dir(pattern='\\.pdf$'))
cat(
paste0(
'<a href="',
files,
'>',
files,
'</a>',
collapse = '\n\n'
)
quarto::quarto_preview_stop()
quarto::quarto_render()
quarto::quarto_preview()
quarto::quarto_preview_stop()
quarto::quarto_preview()
quarto::quarto_preview_stop()
quarto::quarto_preview()
quarto::quarto_preview_stop()
quarto::quarto_render()
